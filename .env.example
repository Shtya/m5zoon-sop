DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/makhzon?schema=public&sslmode=require"
AUTH_SECRET="replace-with-a-random-string-at-least-32-chars-long"

# Required on Vercel (Project → Settings → Environment Variables):
# DATABASE_URL  → same Supabase URL used locally (sslmode=require)
# AUTH_SECRET   → at least 32 characters, Production + Preview
